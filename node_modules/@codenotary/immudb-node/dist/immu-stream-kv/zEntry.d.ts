/// <reference types="node" />
import { Buffer } from 'node:buffer';
/**
 * Deserializes zEntry from list of buffers heaving
 * following structure:
 * - setKey length - UInt64BE
 * - setKey
 * - refKey length - UInt64BE
 * - refKey
 * - refKeyScore length - UInt64BE
 * - refKeyScore - DoubleBE
 * - refKeyAtTxId length - UInt64BE
 * - refKeyAtTxId - UInt64BE
 * - refVal length - UInt64BE
 * - refVal
 */
export declare function toZEntry(everything: Buffer): {
    zSet: Buffer;
    score: number;
    refKeySeenFromTxId: import("long");
    refKey: Buffer;
    referencedKeyVal: Buffer;
};
/**
 * Deserializes zEntries from list of buffers heaving
 * following structure:
 * - serie of:
 *   - setKey length - UInt64BE
 *   - setKey
 *   - refKey length - UInt64BE
 *   - refKey
 *   - refKeyScore length - UInt64BE
 *   - refKeyScore - DoubleBE
 *   - refKeyAtTxId length - UInt64BE
 *   - refKeyAtTxId - UInt64BE
 *   - refVal length - UInt64BE
 *   - refVal
 */
export declare function toZEntries(everything: Buffer): {
    zSet: Buffer;
    score: number;
    refKeySeenFromTxId: import("long");
    refKey: Buffer;
    referencedKeyVal: Buffer;
}[];
//# sourceMappingURL=zEntry.d.ts.map
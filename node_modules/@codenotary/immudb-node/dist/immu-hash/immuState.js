import * as buffer from '../buffer.js';
export function hashOfImmuState(props) {
    return Buffer.concat([
        buffer.fromUInt16BE(props.databaseName.length),
        Buffer.from(props.databaseName),
        Buffer.from(props.lastTransactionId.toBytesBE()),
        props.lastTransactionHash // immu hash
    ]);
}
//# sourceMappingURL=immuState.js.map